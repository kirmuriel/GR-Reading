# Book Stats for GR
Reads the status update RSS from [Goodreads](www.goodreads.com) and displays the information each book information in a table and a chart. Also displays a chart with all the books read up-to-date.  
Originally coded in `PHP`, has a version in `Node.js`, deployed to [nodejitsu](http://www.nodejitsu.com/)
## Tags
Stats, goodreads, node, php

Create lib/config.js

how to update

$ cd $HM/BookPrediction
$ af login
// use key chain
$ af update readingstats
